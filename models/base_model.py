#!/usr/bin/python3
# Ensure correct spacing and indentation

class BaseModel:

    def __init__(self):
        """Instantiates a new model"""
        # Ensure block is properly indented
        if some_condition:
            self.value = some_value

    def another_method(self):
        """Another method in BaseModel"""
        if another_condition:
            self.value = another_value

# Ensure newline at end of file
